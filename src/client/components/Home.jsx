import React from 'react';

const Home = () => {
    return (
        <div>
            <div>Home Works Yes!</div>
            <button onClick={() => console.log('Hi')}>Press Me!</button>
        </div>
    );
};

export default Home;
